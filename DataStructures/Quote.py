class Quote():

    def __init__(self, quote_id, source, text, date, discourse):

        self.source = source
        self.text = text
        self.date = date
        self.sid = quote_id
        self.discourse = discourse